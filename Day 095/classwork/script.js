// 1) ახსენით რა არის: Client, Server, HTTP, TCP, სტატუს კოდი 200 და 404,

// Client არის მოწყობილობა, რომელიც სერვერს უგზავნის მოთხოვნას.
// Server არის კომპიუტერი, რომელიც იღებს Client-ის მოთხოვნას, ამუშავებს მას და აბრუნებს პასუხს.
// HTTP არის პროტოკოლი, რომლის საშუალებითაც ბრაუზერი და სერვერი ერთმანეთთან ინფორმაციას ცვლიან.
// TCP არის პროტოკოლი, რომელიც უზრუნველყობს მონაცემების სანდოდ და სწორად გადაცემას ორ მოწყობილობის შორის.
// 200 ნიშნავს, რომ მოთხოვნა წარმატებით შესრულდა.
// 404 ნიშნავს, რომ მოთხოვნილი რესურსი ვერ მოიძებნა.

// რა განსხვავებაა HTTP-სა და HTTPS-ს შორის?

// HTTP არის პროტოკოლი, რომლის საშუალებითაც ბრაუზერი და სერვერი ერთმანეთთან ინფორმაციას ცვლიან.
// HTTPS იგივეა, უბრალოდ HTTP-ის უსაფრთხო ვერსიაა


// 2) ახსენით რა არის GET და POST

// GET და POST არის HTTP მეთოდები, რომლებსაც Client იყენებს Server-თან მონაცემების გასაგზავნად ან მისაღებად.
// GET გამოიყენება Server-იდან მონაცემების მისაღებად, 
// ხოლო POST - Server-ზე ახალი მონაცემების გასაგზავნად.


// 3) შენ აგრძელებ "Todo" (გასაკეთებელი საქმეების) აპლიკაციის განვითარებას. 
// ამჯერად ახალი დავალების სერვერზე გასაგზავნად უნდა გამოიყენო თანამედროვე ასინქრონული მიდგომა — async/await და try...catch ბლოკი.
// შენი ამოცანაა, დაწერო JavaScript კოდი შემდეგი ინსტრუქციებით:
// ფუნქციის შექმნა:
// შექმენი ასინქრონული ფუნქცია სახელწოდებით addTodo (async).
// შეცდომების მართვის ბლოკი (try...catch):
// ფუნქციის შიგნით გახსენი try...catch კონსტრუქცია, რათა მოსალოდნელი შეცდომები უსაფრთხოდ დაიჭირო.
// მონაცემების მომზადება:
// try ბლოკის შიგნით, აიღე ინპუტში ჩაწერილი მნიშვნელობა (todoInput.value) და შეინახე ცვლადში.
// გადააქციე ეს მონაცემი JSON ტექსტად JSON.stringify()-ის გამოყენებით (გასაღები იყოს task).
// მოთხოვნის გაგზავნა (await fetch):
// შექმენი response ცვლადი და მიანიჭე მას await fetch()-ის შედეგი.
// პირველ არგუმენტად გადაეცი სერვერის მისამართი (url), ხოლო მეორე არგუმენტად — ობიექტი, სადაც მიუთითებ მეთოდს (method: 'POST') და სხეულს (body).
// პასუხის დამუშავება და შეცდომის შემოწმება:
// შეამოწმე, არის თუ არა response.ok ჭეშმარიტი.
// თუ კი, მიიღე და დააბრუნე სერვერის პასუხი await response.json()-ის გამოყენებით.
// თუ response.ok მცდარია, ისროლე ახალი შეცდომა (throw new Error(...)).
// catch ბლოკში კი დაბეჭდე ქსელის შეცდომის მესიჯი კონსოლში (console.log(...)).

const url = 'https://jsonplaceholder.typicode.com/posts'

async function addTodo() {
    try {
        const newTask = todoInput.value
        const data = JSON.stringify({ task: newTask})

        const res = await fetch(url, {
            method: "POST",
            body: data
        })

        if (res.ok) {
            const jres = await res.json()
            console.log(jres)
            return jres
        }

        throw new Error("usdhfgjksedhfgkjdsfg")
    } catch (error) {
        console.log(error.message)
    }
}